declare var webApp: WebsiteApplication;

$(() => {
    webApp = new WebsiteApplication();
});

// Your project's scripts go here.
class WebsiteApplication {

    constructor() { this.initialize(); }

    initialize() {
        // TODO: initialization code.
    }
}